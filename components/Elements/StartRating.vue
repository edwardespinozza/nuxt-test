<template>
  <!-- <div class="d-flex">
    <div class="d-flex me-2">
      <i
        v-for="star of [1,2,3,4,5]"
          :key="`rating-${star}`"
          class="iconf-star me-1"
          :class="
          (body.reviews.average && star <= body.reviews.average) ? 'warnings' : 'medium-gray'
          "
        >
      </i>
    </div>
    <span v-if="body.reviews.average" class="medium-gray">
      {{ body.reviews.average }}
      ({{ body.reviews.total }})
      </span>
    </div> -->
</template>